## Usage

First of all specify your `config.yaml` file with all vars and put path to it into `CONFIG_FILE_PATH` constant

Go to `task` root dir in your PC and paste into your terminal

```bash
cargo install --path .
```
and then :

```bash
task3 stream
```
